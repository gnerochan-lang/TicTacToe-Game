## Packages
framer-motion | Animations for game pieces and board transitions
lucide-react | Icons for UI elements (refresh, trophy, history)
canvas-confetti | For winning celebration effects
@types/canvas-confetti | Types for confetti
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  handwriting: ["var(--font-handwriting)"],
}
